import { StepSelectorDirective } from './step-selector.directive';

describe('StepSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new StepSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
